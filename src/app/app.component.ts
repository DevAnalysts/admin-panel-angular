import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {
 // showHeader: boolean = true; // Initially, the header is visible
  //showSidebar: boolean = true; // Initially, the header is visible
  title = 'Modernize Angular Admin Tempplate';
}
